apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "preloop_ai.fullname" . }}-migration-job
  labels:
    {{- include "preloop_ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      labels:
        {{- include "preloop_ai.selectorLabels" . | nindent 8 }}
    spec:
      restartPolicy: Never
      containers:
        - name: migration-runner
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "Always" }}
          command: ["/bin/sh", "-c"]
          args: ["cd /app/SpaceModels && alembic upgrade head"]
          env:
            - name: DATABASE_URL
              value: {{ include "preloop_ai.databaseUrl" . }}
