apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "spacebridge.fullname" . }}-migration-job
  labels:
    {{- include "spacebridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      labels:
        {{- include "spacebridge.selectorLabels" . | nindent 8 }}
    spec:
      restartPolicy: Never
      containers:
        - name: migration-runner
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          command: ["/bin/sh", "-c"]
          args: ["cd /app/SpaceModels && alembic upgrade head"]
          env:
            - name: DATABASE_URL
              value: {{ .Values.environment.databaseUrl | quote }}
            # Add other necessary environment variables from secrets/configmaps if needed
